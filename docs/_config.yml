# SGAIP Website - Jekyll Configuration
title: SGAIP – Stateless Global Agent Identity Protocol
description: Stateless cryptographic identity for humans and AI agents

# Site URL (custom domain)
url: https://sgaip.org
baseurl: ""   # MUST be empty for custom domain

# Build settings
markdown: kramdown
theme: jekyll-theme-slate

# Plugins supported by GitHub Pages
plugins:
  - jekyll-sitemap
  - jekyll-seo-tag

# Site metadata
author:
  name: SGAIP Contributors

locale: en_US
timezone: UTC

# Safety (GitHub Pages requirement)
safe: true

# Exclude unnecessary files (DO NOT exclude *.md)
exclude:
  - .git
  - .gitignore
  - node_modules
  - Gemfile
  - Gemfile.lock

# Optional includes
include:
  - robots.txt
  - favicon.svg
